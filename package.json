{
  "name": "ubireason",
  "version": "0.1.0",
  "scripts": {
    "build": "bsb -make-world",
    "clean": "bsb -clean-world",
    "deploy": "webpack --config webpack.config.js --progress",
    "dev": "moduleserve ./",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "npm install && npm run build && npm run deploy"
  },
  "keywords": [
    "BuckleScript",
    "ReasonReact",
    "reason-react"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@glennsl/bs-json": "^5.0.1",
    "bs-css": "^11.0.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "reason-react": ">=0.7.0"
  },
  "devDependencies": {
    "bs-platform": "^7.0.1",
    "moduleserve": "^0.9.1",
    "webpack": "^4.41.4",
    "webpack-cli": "^3.3.10"
  },
  "engines": {
    "node": "12.x"
  }
}
